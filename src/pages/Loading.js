function Loading() {
    return (
        <div
            style={{
                display: "flex",
                alignItems: "center",
                justifyContent: "center",
                height: "100vh",
            }}
        >
            <h1 className="font-bold text-7xl">Loading...</h1>
        </div>
    );
}

export default Loading;
